#!/bin/bash
awk 'BEGIN{w="water";s="stir.";b="bestchol";W=ENVIRON["WATER"];S=ENVIRON["STIR"];n=0;for(i=1;i<=length(W);i++){n=n*5+index(w,substr(W,i,1))-1};for(i=1;i<=length(S);i++){n=n*5+index(s,substr(S,i,1))-1};r="";while(n>0){r=substr(b,n%8+1,1) r;n=int(n/8)};print r}' /dev/null
